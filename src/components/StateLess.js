import React from 'react';

const Component = ( props ) => {
    return(
    <div>
        <h3>This is a StateLess Component on React js with {props.name}</h3>
    </div>
    );
}

export default Component